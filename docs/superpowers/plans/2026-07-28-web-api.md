# Web + API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cloudflare Pages で新しい検索 UI と SVG/mxlibrary 配信 API を提供し、Plan A/B の deferred items を解消する。

**Architecture:** Astro SSG + Preact islands + Cloudflare Pages Functions。API は Pages Functions で D1 FTS5 検索 + R2 からの SVG/mx 配信。UI は `packages/ui` の Preact コンポーネント island として hydration。

**Tech Stack:** Astro 5 + Preact 10 + Tailwind CSS v4 + Cloudflare Pages Functions + D1 (FTS5) + R2 + Vitest 4 (+ `@cloudflare/vitest-pool-workers`) + Biome 2

## Global Constraints

- Node 22 LTS / pnpm 9 / TypeScript strict (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`, `allowImportingTsExtensions`, `noImplicitOverride`)
- Bundler: Astro (Vite ベース) / Test: Vitest 4 / Lint & Format: Biome 2
- 依存バージョンは CLI (`pnpm add`) 経由でインストール、`package.json` にハードコードしない
- linter-ignore ディレクティブ (`@ts-ignore`, `@biome-ignore` など) 禁止 — 設計で解消
- `IconHit.width` / `IconHit.height` は API レスポンス上必須。値は `collection_meta.default_width/height` から埋め、デフォルトは 24
- D1 の 100 bind-param 上限 (per statement) を維持
- Cloudflare Pages secrets は Pages env にのみ配置。ローカルは `apps/web/.dev.vars` (gitignore)
- 追加パッケージ / peer 依存は各 package の `package.json` に定義し、workspace root には置かない
- `IconHit` の shape は wire format 互換のため変更しない (`{ collection, name, license, width, height }`)

---

### Task 1: Extract `splitStatements` to `packages/core/src/sql.ts`

**Files:**
- Create: `packages/core/src/sql.ts`
- Create: `packages/core/tests/sql.test.ts`
- Modify: `packages/core/src/index.ts` — export `splitStatements`
- Modify: `tools/ingest/src/schema.ts` — 使用側を書き換え、`/;\s*(?=…)/` の inline regex を除去

**Interfaces:**
- Produces: `export const splitStatements = (sql: string, opts: { keywords: readonly string[] }): readonly string[]`
  - `keywords` は分割トリガとなる SQL 予約語のリスト (例: `['CREATE', 'DROP', 'ALTER', 'BEGIN', 'END']`)
  - 空文字列 / trailing `;` は除外して返す
  - 大文字小文字区別しない
- Consumes: `tools/ingest/src/schema.ts` は `SCHEMA_STATEMENTS = splitStatements(raw, { keywords: ['CREATE', 'DROP', 'ALTER', 'BEGIN', 'END'] })`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// packages/core/tests/sql.test.ts
import { describe, expect, it } from 'vitest';
import { splitStatements } from '../src/sql.ts';

describe('splitStatements', () => {
  it('splits DDL by CREATE keyword regardless of case', () => {
    const sql = 'CREATE TABLE a (id INT);\ncreate INDEX idx ON a(id);';
    expect(splitStatements(sql, { keywords: ['CREATE'] })).toEqual([
      'CREATE TABLE a (id INT)',
      'create INDEX idx ON a(id)',
    ]);
  });

  it('drops empty statements and trailing semicolons', () => {
    const sql = 'CREATE TABLE a (id INT);\n\n;;;\n';
    expect(splitStatements(sql, { keywords: ['CREATE'] })).toEqual([
      'CREATE TABLE a (id INT)',
    ]);
  });

  it('does not split on tokens that only appear inside identifiers', () => {
    const sql = "CREATE TABLE creator (id INT); CREATE TABLE b (id INT);";
    expect(splitStatements(sql, { keywords: ['CREATE'] })).toEqual([
      'CREATE TABLE creator (id INT)',
      'CREATE TABLE b (id INT)',
    ]);
  });

  it('accepts multiple keywords', () => {
    const sql = 'CREATE TABLE a (id INT); DROP TABLE b; ALTER TABLE c ADD COLUMN x INT;';
    expect(
      splitStatements(sql, { keywords: ['CREATE', 'DROP', 'ALTER'] }),
    ).toEqual([
      'CREATE TABLE a (id INT)',
      'DROP TABLE b',
      'ALTER TABLE c ADD COLUMN x INT',
    ]);
  });

  it('returns empty array on empty input', () => {
    expect(splitStatements('', { keywords: ['CREATE'] })).toEqual([]);
    expect(splitStatements('   \n\t  ', { keywords: ['CREATE'] })).toEqual([]);
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/core test`
Expected: 5 tests fail — `splitStatements` not exported.

- [ ] **Step 3: 実装**

```typescript
// packages/core/src/sql.ts
export type SplitOptions = { keywords: readonly string[] };

export const splitStatements = (sql: string, opts: SplitOptions): readonly string[] => {
  if (opts.keywords.length === 0) return sql.trim().length > 0 ? [sql.trim()] : [];
  const escaped = opts.keywords.map((k) => k.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'));
  const pattern = new RegExp(`;\\s*(?=(?:${escaped.join('|')})\\b)`, 'i');
  return sql
    .split(pattern)
    .map((s) => s.replace(/;\s*$/, '').trim())
    .filter((s) => s.length > 0);
};
```

- [ ] **Step 4: index.ts に export 追加**

```typescript
// packages/core/src/index.ts に追記
export { splitStatements } from './sql.ts';
export type { SplitOptions } from './sql.ts';
```

- [ ] **Step 5: 使用側を書き換え**

```typescript
// tools/ingest/src/schema.ts (置き換え)
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { splitStatements } from '@icon-collection/core';
import type { D1Client } from './d1.ts';

const schemaPath = fileURLToPath(new URL('./schema.sql', import.meta.url));
const raw = await readFile(schemaPath, 'utf-8');

/** DDL only — split on CREATE/DROP/ALTER/BEGIN/END keywords. */
export const SCHEMA_STATEMENTS: readonly string[] = splitStatements(raw, {
  keywords: ['CREATE', 'DROP', 'ALTER', 'BEGIN', 'END'],
});

export const applySchema = async (d1: D1Client): Promise<void> => {
  for (const stmt of SCHEMA_STATEMENTS) {
    await d1.execute(stmt);
  }
};
```

- [ ] **Step 6: `@icon-collection/core` を ingest から使うため peer 追加**

Run: `pnpm -F @icon-collection/ingest add @icon-collection/core@workspace:^`

- [ ] **Step 7: テスト・型・lint 通過確認**

Run: `pnpm -F @icon-collection/core test && pnpm -F @icon-collection/ingest test && pnpm typecheck && pnpm lint`
Expected: PASS 全件、typecheck / lint clean。

- [ ] **Step 8: commit**

```bash
git add packages/core tools/ingest
git commit -m "refactor(core): extract splitStatements helper, share with ingest"
```

---

### Task 2: `collection_meta` に default_width / default_height を追加

**Files:**
- Modify: `tools/ingest/src/schema.sql`
- Modify: `tools/ingest/src/seed-meta.ts`
- Modify: `tools/ingest/tests/seed-meta.test.ts`
- Modify: `tools/ingest/src/types.ts` — `CollectionSnapshot` に `defaultWidth: number; defaultHeight: number` 追加
- Modify: `tools/ingest/src/collect.ts` — Iconify JSON `body.width` / `body.height` を読んで snapshot に載せる (fallback 24)
- Modify: `tools/ingest/tests/collect.test.ts` — 上記 assert 追加

**Interfaces:**
- Consumes: Iconify JSON の `width` / `height` (top-level, optional)
- Produces: `collection_meta (collection, version, license, total, default_width, default_height, synced_at)` の追加カラム。API 側 (Task 10) が SELECT する

- [ ] **Step 1: 失敗するテストを書く (seed-meta)**

```typescript
// tools/ingest/tests/seed-meta.test.ts に追加
import { describe, expect, it, vi } from 'vitest';
import { seedCollectionMeta } from '../src/seed-meta.ts';
import type { CollectionSnapshot } from '../src/types.ts';

const makeSnap = (over: Partial<CollectionSnapshot> = {}): CollectionSnapshot => ({
  collection: 'mdi',
  version: '1.0.0',
  license: 'Apache-2.0',
  total: 10,
  defaultWidth: 24,
  defaultHeight: 24,
  body: { prefix: 'mdi', icons: {}, info: {} as never },
  ...over,
});

it('writes default_width and default_height', async () => {
  const execute = vi.fn(async () => ({ success: true, meta: { changes: 1, last_row_id: null }, results: [] }));
  const d1 = { execute } as unknown as import('../src/d1.ts').D1Client;
  await seedCollectionMeta({ d1, snapshots: [makeSnap({ defaultWidth: 32, defaultHeight: 32 })] });
  const [, params] = execute.mock.calls[0]!;
  expect(params).toEqual(['mdi', '1.0.0', 'Apache-2.0', 10, 32, 32, expect.any(Number)]);
});
```

- [ ] **Step 2: 失敗するテストを書く (collect)**

```typescript
// tools/ingest/tests/collect.test.ts に追加
it('extracts default width/height from Iconify JSON, falling back to 24', async () => {
  const cases = [
    { body: { prefix: 'a', info: {}, icons: {}, width: 32, height: 32 }, expected: [32, 32] },
    { body: { prefix: 'b', info: {}, icons: {} }, expected: [24, 24] },
  ] as const;
  for (const c of cases) {
    const snap = await collectFromIconify({ collection: c.body.prefix, load: async () => c.body as never });
    expect([snap.defaultWidth, snap.defaultHeight]).toEqual(c.expected);
  }
});
```

- [ ] **Step 3: 失敗を確認**

Run: `pnpm -F @icon-collection/ingest test`
Expected: 上記 2 テストが fail。

- [ ] **Step 4: schema.sql 変更**

```sql
-- tools/ingest/src/schema.sql の collection_meta を置き換え
CREATE TABLE IF NOT EXISTS collection_meta (
  collection     TEXT PRIMARY KEY,
  version        TEXT NOT NULL,
  license        TEXT NOT NULL,
  total          INTEGER NOT NULL,
  default_width  INTEGER NOT NULL DEFAULT 24,
  default_height INTEGER NOT NULL DEFAULT 24,
  synced_at      INTEGER NOT NULL
);
```

- [ ] **Step 5: types.ts に defaultWidth / defaultHeight を追加**

```typescript
// tools/ingest/src/types.ts の CollectionSnapshot を置き換え
export type CollectionSnapshot = {
  collection: string;
  version: string;
  license: string;
  total: number;
  defaultWidth: number;
  defaultHeight: number;
  body: IconifyJSON;
};
```

- [ ] **Step 6: collect.ts で width/height を読む**

```typescript
// tools/ingest/src/collect.ts collectFromIconify 内の snapshot 組立部分を修正
const body = await input.load(input.collection);
const defaultWidth = typeof body.width === 'number' && body.width > 0 ? body.width : 24;
const defaultHeight = typeof body.height === 'number' && body.height > 0 ? body.height : 24;
return {
  collection: input.collection,
  version: body.info?.version ?? '0.0.0',
  license: body.info?.license?.spdx ?? 'UNKNOWN',
  total: Object.keys(body.icons ?? {}).length,
  defaultWidth,
  defaultHeight,
  body,
};
```

- [ ] **Step 7: seed-meta.ts を更新**

```typescript
// tools/ingest/src/seed-meta.ts の SQL / params を書き換え
const sql = `INSERT INTO collection_meta (collection, version, license, total, default_width, default_height, synced_at) VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT(collection) DO UPDATE SET version = excluded.version, license = excluded.license, total = excluded.total, default_width = excluded.default_width, default_height = excluded.default_height, synced_at = excluded.synced_at`;
const now = Math.floor(Date.now() / 1000);
let upserted = 0;
for (const snap of input.snapshots) {
  await input.d1.execute(sql, [
    snap.collection,
    snap.version,
    snap.license,
    snap.total,
    snap.defaultWidth,
    snap.defaultHeight,
    now,
  ]);
  upserted++;
}
```

- [ ] **Step 8: テスト通過確認**

Run: `pnpm -F @icon-collection/ingest test`
Expected: PASS 全件。

- [ ] **Step 9: typecheck / lint**

Run: `pnpm typecheck && pnpm lint`
Expected: clean。

- [ ] **Step 10: commit**

```bash
git add tools/ingest
git commit -m "feat(ingest): store default width/height per collection"
```

---

### Task 3: `D1Client.batchAtomic` を追加し、seed-icons / seed-synonyms を原子化

**Files:**
- Modify: `tools/ingest/src/d1.ts`
- Modify: `tools/ingest/tests/d1.test.ts`
- Modify: `tools/ingest/src/seed-icons.ts`
- Modify: `tools/ingest/src/seed-synonyms.ts`
- Modify: `tools/ingest/tests/seed-icons.test.ts` / `tests/seed-synonyms.test.ts`

**Interfaces:**
- Produces: `D1Client.batchAtomic(statements: readonly { sql: string; params?: readonly unknown[] }[]): Promise<D1Result[]>`
  - Cloudflare D1 HTTP `POST /accounts/{id}/d1/database/{db}/query` に body を JSON 配列 (`[{sql, params}, ...]`) として送る (D1 側で単一 tx 化される)
  - `body.result` は文の順に返る `D1Result[]`
  - 失敗時は既存 `D1Error` を throw
- Consumes: `seed-icons` / `seed-synonyms` は既存 `d1.batch(...)` の call site を `d1.batchAtomic(...)` に置き換える (per-collection の DELETE + N 個の INSERT を 1 tx にまとめる)

- [ ] **Step 1: 失敗するテストを書く (d1)**

```typescript
// tools/ingest/tests/d1.test.ts に追加
import { D1Client } from '../src/d1.ts';

it('batchAtomic posts statements as JSON array and returns per-statement results', async () => {
  const seen: unknown[] = [];
  const fetchImpl = async (url: string, init?: RequestInit) => {
    seen.push({ url, body: init?.body });
    return new Response(
      JSON.stringify({
        success: true,
        result: [
          { success: true, meta: { changes: 3, last_row_id: null }, results: [] },
          { success: true, meta: { changes: 1, last_row_id: 42 }, results: [] },
        ],
      }),
      { status: 200 },
    );
  };
  const client = new D1Client(
    { apiToken: 't', accountId: 'a', databaseId: 'd' },
    fetchImpl as typeof fetch,
  );
  const results = await client.batchAtomic([
    { sql: 'DELETE FROM icons WHERE collection = ?', params: ['mdi'] },
    { sql: 'INSERT INTO icons (id, collection, name, license, updated_at) VALUES (?, ?, ?, ?, ?)', params: [1, 'mdi', 'home', 'Apache-2.0', 100] },
  ]);
  expect(results.map((r) => r.meta.changes)).toEqual([3, 1]);
  const body = JSON.parse((seen[0] as { body: string }).body);
  expect(Array.isArray(body)).toBe(true);
  expect(body).toHaveLength(2);
  expect(body[0].sql).toBe('DELETE FROM icons WHERE collection = ?');
  expect(body[1].params).toEqual([1, 'mdi', 'home', 'Apache-2.0', 100]);
});

it('batchAtomic throws D1Error on non-success', async () => {
  const fetchImpl = async () =>
    new Response(JSON.stringify({ success: false, errors: [{ code: 8000, message: 'x' }] }), { status: 500 });
  const client = new D1Client(
    { apiToken: 't', accountId: 'a', databaseId: 'd' },
    fetchImpl as typeof fetch,
  );
  await expect(client.batchAtomic([{ sql: 'DELETE FROM icons' }])).rejects.toThrow('D1 request failed');
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/ingest test -- tests/d1.test.ts`
Expected: 2 tests fail (`batchAtomic` not defined)。

- [ ] **Step 3: 実装**

```typescript
// tools/ingest/src/d1.ts の D1Client 内に追加
async batchAtomic(
  statements: readonly { sql: string; params?: readonly unknown[] }[],
): Promise<D1Result[]> {
  if (statements.length === 0) return [];
  const res = await this.fetchImpl(this.endpoint, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${this.cfg.apiToken}`,
      'content-type': 'application/json',
    },
    body: JSON.stringify(statements.map((s) => ({ sql: s.sql, params: s.params ?? [] }))),
  });
  const body = (await res.json()) as D1Response;
  if (!res.ok || !body.success) {
    throw new D1Error({
      status: res.status,
      errors: body.errors ?? [{ code: -1, message: 'unknown' }],
    });
  }
  if (!body.result || body.result.length !== statements.length) {
    throw new D1Error({
      status: res.status,
      errors: [{ code: -3, message: `result count mismatch: expected ${statements.length}` }],
    });
  }
  return body.result;
}
```

- [ ] **Step 4: 失敗するテストを書く (seed-icons: batchAtomic 呼び出し)**

```typescript
// tools/ingest/tests/seed-icons.test.ts に追加
it('uses batchAtomic to combine DELETE and INSERT per collection', async () => {
  const batchAtomic = vi.fn(async (stmts: unknown[]) =>
    stmts.map(() => ({ success: true, meta: { changes: 1, last_row_id: null }, results: [] })),
  );
  const execute = vi.fn(async () => ({ success: true, meta: { changes: 0, last_row_id: null }, results: [] }));
  const d1 = { execute, batchAtomic } as unknown as import('../src/d1.ts').D1Client;
  await seedIcons({ d1, snapshots: [/* 2-icon snapshot */] });
  expect(batchAtomic).toHaveBeenCalled();
  const firstBatch = batchAtomic.mock.calls[0]![0] as ReadonlyArray<{ sql: string }>;
  expect(firstBatch[0]!.sql).toContain('DELETE FROM icons');
});
```

- [ ] **Step 5: 失敗を確認 → 実装**

```typescript
// tools/ingest/src/seed-icons.ts の per-collection ループ内
// (置き換え：DELETE と各 INSERT バッチを 1 回の batchAtomic に統合)
const chunks: string[][] = /* 既存の chunk 分割 */;
const statements: { sql: string; params: readonly unknown[] }[] = [
  { sql: `DELETE FROM icons WHERE collection = ?`, params: [snap.collection] },
];
for (const chunk of chunks) {
  const placeholders = chunk.map(() => '(?, ?, ?, ?, ?, ?, ?)').join(', ');
  const params: unknown[] = [];
  for (const name of chunk) {
    /* push per-row 7 params */
  }
  statements.push({
    sql: `INSERT INTO icons (id, collection, name, license, categories, tags, updated_at) VALUES ${placeholders}`,
    params,
  });
}
const results = await input.d1.batchAtomic(statements);
inserted += results.slice(1).reduce((n, r) => n + r.meta.changes, 0);
```

同様に `seed-synonyms.ts` も DELETE + INSERT を 1 回の `batchAtomic` にまとめる。

- [ ] **Step 6: 既存の `batch()` メソッドの位置付け更新**

`batch()` は「非原子・逐次」の意味に留めるか、削除して `batchAtomic` に一本化するかを判断。テストで使用箇所を grep して、他に呼び出し元がなければ削除。

Run: `grep -rn "\.batch(" tools/ingest/src`
- 呼び出しが `batchAtomic` のみになるなら `batch` メソッド + 対応テストを削除。

- [ ] **Step 7: テスト通過確認**

Run: `pnpm -F @icon-collection/ingest test`
Expected: PASS 全件。

- [ ] **Step 8: typecheck / lint**

Run: `pnpm typecheck && pnpm lint`
Expected: clean。

- [ ] **Step 9: commit**

```bash
git add tools/ingest
git commit -m "feat(ingest): run DELETE+INSERT batch atomically via D1 batch endpoint"
```

---

### Task 4: `packages/ui/src/svg-cache.ts` factory を追加し、`IconCell` のモジュールグローバルを排除

**Files:**
- Create: `packages/ui/src/svg-cache.ts`
- Create: `packages/ui/tests/svg-cache.test.ts`
- Modify: `packages/ui/src/host.tsx` — `Host` 型に `svgCache: SvgCache` を追加
- Modify: `packages/ui/src/IconCell.tsx` — module-level `svgCache` 削除、`host.svgCache` を使用
- Modify: `packages/ui/src/index.ts` — `SvgCache` と `createSvgCache` を re-export

**Interfaces:**
- Produces:
  ```typescript
  export type SvgCache = {
    get: (key: string) => string | undefined;
    set: (key: string, value: string) => void;
    has: (key: string) => boolean;
    readonly size: number;
  };
  export const createSvgCache = (): SvgCache;
  ```
- `IconCell` は `useHost().svgCache` を通じてキャッシュを共有する

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// packages/ui/tests/svg-cache.test.ts
import { describe, expect, it } from 'vitest';
import { createSvgCache } from '../src/svg-cache.ts';

describe('createSvgCache', () => {
  it('stores and retrieves values by key', () => {
    const c = createSvgCache();
    expect(c.has('a')).toBe(false);
    c.set('a', '<svg />');
    expect(c.get('a')).toBe('<svg />');
    expect(c.has('a')).toBe(true);
    expect(c.size).toBe(1);
  });

  it('is isolated between instances', () => {
    const a = createSvgCache();
    const b = createSvgCache();
    a.set('x', 'X');
    expect(b.get('x')).toBeUndefined();
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/ui test`
Expected: fail — `svg-cache.ts` missing.

- [ ] **Step 3: 実装**

```typescript
// packages/ui/src/svg-cache.ts
export type SvgCache = {
  get: (key: string) => string | undefined;
  set: (key: string, value: string) => void;
  has: (key: string) => boolean;
  readonly size: number;
};

export const createSvgCache = (): SvgCache => {
  const store = new Map<string, string>();
  return {
    get: (k) => store.get(k),
    set: (k, v) => {
      store.set(k, v);
    },
    has: (k) => store.has(k),
    get size() {
      return store.size;
    },
  };
};
```

- [ ] **Step 4: Host 型と HostProvider を拡張**

```typescript
// packages/ui/src/host.tsx の Host に追加
import type { SvgCache } from './svg-cache.ts';
export type { SvgCache };
export type Host = {
  apiBaseUrl: string;
  apiClient?: ApiClient; // Task 5 で必須化される想定
  svgCache: SvgCache;
  copyText: (s: string) => Promise<void>;
  showToast: (m: string) => void;
  persistState: {
    get: (k: string) => Promise<string | null>;
    set: (k: string, v: string) => Promise<void>;
  };
};
```

注: `apiClient` の必須化は Task 5 で行う。本タスクでは `svgCache` の追加のみ。

- [ ] **Step 5: IconCell から module-global を除去**

```typescript
// packages/ui/src/IconCell.tsx の変更
// 削除: const svgCache = new Map<string, string>();
// 内部で host.svgCache を参照する
const [status, setStatus] = useState<CellStatus>(() =>
  host.svgCache.has(cacheKey(hit)) ? 'ready' : 'idle',
);
const [svg, setSvg] = useState<string | null>(() => host.svgCache.get(cacheKey(hit)) ?? null);
/* … */
host.svgCache.set(cacheKey(hit), sanitized);
```

- [ ] **Step 6: 既存 IconCell テスト（もしあれば）を更新して host に svgCache を渡す**

- [ ] **Step 7: index.ts に export 追加**

```typescript
// packages/ui/src/index.ts
export type { SvgCache } from './svg-cache.ts';
export { createSvgCache } from './svg-cache.ts';
```

- [ ] **Step 8: テスト / typecheck / lint**

Run: `pnpm -F @icon-collection/ui test && pnpm typecheck && pnpm lint`
Expected: PASS 全件、clean。

- [ ] **Step 9: commit**

```bash
git add packages/ui
git commit -m "refactor(ui): move svg cache into HostProvider-scoped factory"
```

---

### Task 5: `Host.apiClient` を必須にし、`useCopy` / `useSearch` / `IconCell` から `createApiClient` 直呼びを排除

**Files:**
- Modify: `packages/ui/src/host.tsx` — `apiClient: ApiClient` を必須化 (`apiBaseUrl` は残す)
- Modify: `packages/ui/src/hooks/useCopy.ts`
- Modify: `packages/ui/src/hooks/useSearch.ts`
- Modify: `packages/ui/src/IconCell.tsx`
- Update: `packages/ui/tests/**` — Host mock に `apiClient` を追加

**Interfaces:**
- Consumes: `Host.apiClient: ApiClient` (from `@icon-collection/core`)
- `useCopy` / `useSearch` / `IconCell` は `host.apiClient` を参照するだけ (メモ化不要)

- [ ] **Step 1: Host 型変更**

```typescript
// packages/ui/src/host.tsx
import type { ApiClient } from '@icon-collection/core';
export type Host = {
  apiBaseUrl: string;
  apiClient: ApiClient;
  svgCache: SvgCache;
  copyText: (s: string) => Promise<void>;
  showToast: (m: string) => void;
  persistState: {
    get: (k: string) => Promise<string | null>;
    set: (k: string, v: string) => Promise<void>;
  };
};
```

- [ ] **Step 2: 失敗するテストを書く**

```typescript
// packages/ui/tests/hooks-injection.test.ts (新規)
import { renderHook } from '@testing-library/preact';
import { describe, expect, it, vi } from 'vitest';
import { HostProvider, useSearch, createSvgCache } from '../src/index.ts';
import type { Host } from '../src/host.tsx';
import type { ComponentChildren } from 'preact';

it('useSearch calls host.apiClient.search (no createApiClient inside)', async () => {
  const search = vi.fn(async () => ({ hits: [], total: 0, cursor: null }));
  const host: Host = {
    apiBaseUrl: 'https://example.test',
    apiClient: {
      search,
      getSvg: async () => '',
      getMx: async () => '',
    },
    svgCache: createSvgCache(),
    copyText: async () => undefined,
    showToast: () => undefined,
    persistState: { get: async () => null, set: async () => undefined },
  };
  const wrapper = ({ children }: { children: ComponentChildren }) => (
    <HostProvider host={host}>{children}</HostProvider>
  );
  const { result } = renderHook(() => useSearch({ q: 'x' }), { wrapper });
  await vi.waitFor(() => expect(search).toHaveBeenCalled());
  expect(search).toHaveBeenCalledWith({ q: 'x' });
});
```

- [ ] **Step 3: 失敗を確認**

Run: `pnpm -F @icon-collection/ui test`
Expected: fail — 現在は `createApiClient` を内部で呼ぶため mock が刺さらない。

- [ ] **Step 4: hooks / IconCell の書き換え**

```typescript
// packages/ui/src/hooks/useCopy.ts
export const useCopy = (): ((kind: CopyKind, hit: IconHit) => Promise<void>) => {
  const host = useHost();
  return async (kind, hit) => {
    try {
      const client = host.apiClient;
      if (kind === 'svg') {
        const svg = await client.getSvg(hit.collection, hit.name);
        await host.copyText(svg);
      } else if (kind === 'jsx') {
        const svg = await client.getSvg(hit.collection, hit.name);
        await host.copyText(svgToJsx(svg));
      } else {
        const mx = await client.getMx(hit.collection, hit.name);
        await host.copyText(mx);
      }
      host.showToast('Copied');
    } catch (e) {
      // Task 6 で message を surface する。まずは以前通りの UX を維持
      host.showToast('Copy failed');
    }
  };
};
```

```typescript
// packages/ui/src/hooks/useSearch.ts (useMemo(createApiClient) を削除)
const host = useHost();
const client = host.apiClient;
```

```typescript
// packages/ui/src/IconCell.tsx (useMemo(createApiClient) を削除)
const host = useHost();
const client = host.apiClient;
```

- [ ] **Step 5: `@icon-collection/core` に対する UI の直接依存を確認**

`createApiClient` を UI から import しない。UI は `ApiClient` 型のみ import。

Run: `grep -rn "createApiClient" packages/ui/src`
Expected: 0 件。

- [ ] **Step 6: 既存 UI テスト の Host mock を更新**

すべての Host mock に `apiClient` を注入するヘルパを `packages/ui/tests/_helpers.ts` に追加:

```typescript
import type { ApiClient } from '@icon-collection/core';
import type { Host } from '../src/host.tsx';
import { createSvgCache } from '../src/svg-cache.ts';

export const makeHost = (over: Partial<Host> = {}): Host => ({
  apiBaseUrl: 'https://example.test',
  apiClient: {
    search: async () => ({ hits: [], total: 0, cursor: null }),
    getSvg: async () => '<svg/>',
    getMx: async () => '<mx/>',
    ...(over.apiClient ?? {}),
  } as ApiClient,
  svgCache: createSvgCache(),
  copyText: async () => undefined,
  showToast: () => undefined,
  persistState: { get: async () => null, set: async () => undefined },
  ...over,
});
```

既存テストの `host` 生成を `makeHost(...)` に置き換え。

- [ ] **Step 7: テスト / typecheck / lint**

Run: `pnpm -F @icon-collection/ui test && pnpm typecheck && pnpm lint`
Expected: PASS。

- [ ] **Step 8: commit**

```bash
git add packages/ui
git commit -m "refactor(ui): inject ApiClient via HostProvider"
```

---

### Task 6: `useCopy` にエラー message を surface

**Files:**
- Modify: `packages/ui/src/hooks/useCopy.ts`
- Modify: `packages/ui/tests/useCopy.test.ts` (無ければ新規)

**Interfaces:**
- Consumes: `Host.showToast(m: string)` に失敗時 `"Copy failed: ${message}"` を渡す

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// packages/ui/tests/useCopy.test.ts
import { renderHook, act } from '@testing-library/preact';
import { describe, expect, it, vi } from 'vitest';
import { HostProvider, useCopy } from '../src/index.ts';
import { makeHost } from './_helpers.ts';

it('surfaces error message to showToast on failure', async () => {
  const showToast = vi.fn();
  const host = makeHost({
    showToast,
    apiClient: {
      search: async () => ({ hits: [], total: 0, cursor: null }),
      getSvg: async () => {
        throw new Error('boom');
      },
      getMx: async () => '',
    },
  });
  const wrapper = ({ children }: { children: preact.ComponentChildren }) => (
    <HostProvider host={host}>{children}</HostProvider>
  );
  const { result } = renderHook(() => useCopy(), { wrapper });
  await act(async () => {
    await result.current('svg', {
      collection: 'mdi',
      name: 'home',
      license: 'Apache-2.0',
      width: 24,
      height: 24,
    });
  });
  expect(showToast).toHaveBeenCalledWith('Copy failed: boom');
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/ui test`
Expected: fail — 現在は `'Copy failed'` (message 無し)。

- [ ] **Step 3: 実装**

```typescript
// packages/ui/src/hooks/useCopy.ts の catch 節を書き換え
} catch (e) {
  const message = e instanceof Error ? e.message : String(e);
  host.showToast(`Copy failed: ${message}`);
}
```

- [ ] **Step 4: テスト通過確認**

Run: `pnpm -F @icon-collection/ui test`
Expected: PASS 全件。

- [ ] **Step 5: commit**

```bash
git add packages/ui
git commit -m "feat(ui): surface useCopy error message via showToast"
```

---

### Task 7: `apps/web` scaffold (Astro + Preact + Tailwind v4)

**Files:**
- Create: `apps/web/package.json`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/astro.config.mjs`
- Create: `apps/web/vitest.config.ts`
- Create: `apps/web/src/env.d.ts`
- Create: `apps/web/src/styles/global.css`
- Create: `apps/web/src/layouts/BaseLayout.astro`
- Create: `apps/web/src/pages/index.astro` (最小版; hydration は Task 13 で詳細化)
- Create: `apps/web/src/islands/SearchPage.tsx` (最小版; Task 13 で詳細化)
- Modify: `pnpm-workspace.yaml` — 既に `apps/*` があるので不要

**Interfaces:**
- `apps/web` は `@icon-collection/ui` / `@icon-collection/core` / `@icon-collection/synonyms` を workspace 依存として使う
- Tailwind v4: `@tailwindcss/vite` プラグイン経由でビルド、`global.css` は `@import "tailwindcss";` から始まる

- [ ] **Step 1: 依存追加**

Run:
```bash
pnpm -F @icon-collection/web add astro @astrojs/preact @astrojs/cloudflare preact
pnpm -F @icon-collection/web add -D @tailwindcss/vite tailwindcss @icon-collection/ui@workspace:^ @icon-collection/core@workspace:^ @icon-collection/synonyms@workspace:^ vitest @cloudflare/workers-types
```

初回のため `apps/web/package.json` を先に作る必要あり:

```json
{
  "name": "@icon-collection/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "typecheck": "astro check && tsc -p . --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

- [ ] **Step 2: tsconfig.json**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "vite/client"],
    "jsx": "preserve",
    "jsxImportSource": "preact"
  },
  "include": ["src/**/*", "functions/**/*", "tests/**/*", "*.config.*", ".astro/types.d.ts"]
}
```

- [ ] **Step 3: astro.config.mjs**

```js
import { defineConfig } from 'astro/config';
import preact from '@astrojs/preact';
import cloudflare from '@astrojs/cloudflare';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  output: 'static',
  adapter: cloudflare({ mode: 'directory' }),
  integrations: [preact()],
  vite: { plugins: [tailwindcss()] },
});
```

- [ ] **Step 4: vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config';
import preact from '@preact/preset-vite';

export default defineConfig({
  plugins: [preact()],
  test: {
    environment: 'happy-dom',
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
  },
});
```

- [ ] **Step 5: styles/global.css**

```css
@import "tailwindcss";
@import "@icon-collection/ui/styles.css";
```

- [ ] **Step 6: env.d.ts**

```typescript
/// <reference types="astro/client" />
/// <reference types="@cloudflare/workers-types" />

interface Env {
  DB: D1Database;
  ICONS: R2Bucket;
  ICON_DEFAULT_WIDTH?: string;
}
```

- [ ] **Step 7: BaseLayout.astro (最小)**

```astro
---
import "../styles/global.css";
export interface Props { title: string }
const { title } = Astro.props;
---
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body class="min-h-dvh bg-neutral-50 text-neutral-900">
    <slot />
  </body>
</html>
```

- [ ] **Step 8: pages/index.astro (プレースホルダ)**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SearchPage } from '../islands/SearchPage.tsx';
---
<BaseLayout title="IconCollection">
  <main class="mx-auto max-w-6xl p-6">
    <h1 class="text-2xl font-semibold">IconCollection</h1>
    <SearchPage client:load apiBaseUrl="" />
  </main>
</BaseLayout>
```

- [ ] **Step 9: islands/SearchPage.tsx (プレースホルダ; Task 13 で拡張)**

```tsx
export type SearchPageProps = { apiBaseUrl: string };
export const SearchPage = (_: SearchPageProps) => <div>placeholder</div>;
```

- [ ] **Step 10: build 通過確認**

Run: `pnpm -F @icon-collection/web build`
Expected: exit code 0。`apps/web/dist/` が生成される。

Run: `pnpm typecheck && pnpm lint`
Expected: clean。

- [ ] **Step 11: commit**

```bash
git add apps/web
git commit -m "feat(web): scaffold Astro + Preact + Tailwind v4 app"
```

---

### Task 8: Cloudflare Pages 設定 (`wrangler.toml` + `.dev.vars.example`)

**Files:**
- Create: `apps/web/wrangler.toml`
- Create: `apps/web/.dev.vars.example`
- Modify: `.gitignore` (root) — `apps/web/.dev.vars` を無視、`apps/web/dist/` は既に無視

**Interfaces:**
- Bindings: `DB` (D1), `ICONS` (R2)。値はダッシュボード側で登録し、`wrangler.toml` には `database_name` / `bucket_name` 等の識別子だけ書く

- [ ] **Step 1: wrangler.toml**

```toml
name = "icon-collection-web"
compatibility_date = "2026-01-01"
pages_build_output_dir = "./dist"

[[d1_databases]]
binding = "DB"
database_name = "icon-collection"
database_id = ""  # ダッシュボードで発行後、CF Pages の env に設定

[[r2_buckets]]
binding = "ICONS"
bucket_name = "icon-collection"
```

- [ ] **Step 2: .dev.vars.example**

```dotenv
# apps/web/.dev.vars.example
# Copy to apps/web/.dev.vars for local wrangler dev (gitignored)
# D1_DATABASE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# R2_BUCKET=icon-collection
```

- [ ] **Step 3: .gitignore 更新**

```
# apps/web/.gitignore に追記 (無ければ作成)
.dev.vars
dist/
.astro/
```

- [ ] **Step 4: wrangler CLI で dry-run 検証**

Run: `pnpm dlx wrangler pages functions build --outdir=./apps/web/dist/_worker.js --experimental-json-config`
- (Astro Cloudflare adapter が生成する `_worker.js` 前提の検証; 失敗しても Task 10-12 の実装後に再検証で OK)

- [ ] **Step 5: commit**

```bash
git add apps/web/wrangler.toml apps/web/.dev.vars.example apps/web/.gitignore
git commit -m "chore(web): add wrangler config and dev-vars template"
```

---

### Task 9: `@cloudflare/vitest-pool-workers` 導入と共通 fixture 準備

**Files:**
- Modify: `apps/web/package.json` — devDeps に `@cloudflare/vitest-pool-workers` を追加
- Create: `apps/web/tests/setup/miniflare.ts`
- Create: `apps/web/tests/setup/seed.sql`
- Modify: `apps/web/vitest.config.ts` — `poolOptions.workers` を設定

**Interfaces:**
- テスト内で `env.DB` / `env.ICONS` にアクセスできる (`@cloudflare/vitest-pool-workers` の `env` を import)
- `seed.sql` は Task 2 の schema と data 少量 (mdi/home, lucide/home 各 1 件、collection_meta 2 件、synonym 1 件) を含む

- [ ] **Step 1: 依存追加**

Run: `pnpm -F @icon-collection/web add -D @cloudflare/vitest-pool-workers`

- [ ] **Step 2: seed.sql**

```sql
CREATE TABLE IF NOT EXISTS icons (
  id INTEGER PRIMARY KEY, collection TEXT NOT NULL, name TEXT NOT NULL,
  license TEXT NOT NULL, categories TEXT, tags TEXT, aliases TEXT,
  updated_at INTEGER NOT NULL, UNIQUE(collection, name)
);
CREATE VIRTUAL TABLE IF NOT EXISTS icons_fts USING fts5(
  name, aliases, tags, categories, collection UNINDEXED,
  content='icons', content_rowid='id',
  tokenize='unicode61 remove_diacritics 2'
);
CREATE TABLE IF NOT EXISTS collection_meta (
  collection TEXT PRIMARY KEY, version TEXT NOT NULL, license TEXT NOT NULL,
  total INTEGER NOT NULL, default_width INTEGER NOT NULL DEFAULT 24,
  default_height INTEGER NOT NULL DEFAULT 24, synced_at INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS synonyms (
  term TEXT NOT NULL, expansion TEXT NOT NULL, lang TEXT NOT NULL,
  weight REAL NOT NULL DEFAULT 1.0, PRIMARY KEY(term, expansion, lang)
);
INSERT INTO collection_meta VALUES ('mdi', '1.0', 'Apache-2.0', 1, 24, 24, 1);
INSERT INTO collection_meta VALUES ('lucide', '1.0', 'ISC', 1, 24, 24, 1);
INSERT INTO icons (id, collection, name, license, categories, tags, aliases, updated_at)
  VALUES (1, 'mdi', 'home', 'Apache-2.0', 'places', 'building', 'house', 1);
INSERT INTO icons (id, collection, name, license, categories, tags, aliases, updated_at)
  VALUES (2, 'lucide', 'home', 'ISC', 'places', 'building', NULL, 1);
INSERT INTO icons_fts(icons_fts) VALUES('rebuild');
INSERT INTO synonyms VALUES ('house', 'home', 'en', 1.0);
```

- [ ] **Step 3: vitest.config.ts を Workers pool に切替**

```typescript
import { defineWorkersConfig } from '@cloudflare/vitest-pool-workers/config';

export default defineWorkersConfig({
  test: {
    poolOptions: {
      workers: {
        singleWorker: true,
        wrangler: { configPath: './wrangler.toml' },
        miniflare: {
          compatibilityDate: '2026-01-01',
          d1Databases: ['DB'],
          r2Buckets: ['ICONS'],
        },
      },
    },
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
  },
});
```

- [ ] **Step 4: setup helper**

```typescript
// apps/web/tests/setup/miniflare.ts
import { env } from 'cloudflare:test';
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';

const seedPath = fileURLToPath(new URL('./seed.sql', import.meta.url));

export const seedTestDb = async (): Promise<void> => {
  const sql = await readFile(seedPath, 'utf-8');
  const stmts = sql.split(/;\s*(?=(?:CREATE|INSERT)\b)/i).map((s) => s.trim()).filter(Boolean);
  for (const s of stmts) await env.DB.prepare(s.replace(/;\s*$/, '')).run();
};

export const putIconFixture = async (collection: string, body: unknown): Promise<void> => {
  await env.ICONS.put(`iconify/${collection}.json`, JSON.stringify(body), {
    httpMetadata: { contentType: 'application/json' },
  });
};
```

- [ ] **Step 5: smoke test**

```typescript
// apps/web/tests/setup.smoke.test.ts
import { env } from 'cloudflare:test';
import { describe, expect, it } from 'vitest';
import { seedTestDb } from './setup/miniflare.ts';

describe('miniflare setup', () => {
  it('creates DB and inserts seed rows', async () => {
    await seedTestDb();
    const rows = await env.DB.prepare('SELECT COUNT(*) as n FROM icons').all();
    expect((rows.results[0] as { n: number }).n).toBe(2);
  });
});
```

- [ ] **Step 6: 実行**

Run: `pnpm -F @icon-collection/web test`
Expected: PASS。

- [ ] **Step 7: commit**

```bash
git add apps/web
git commit -m "test(web): wire vitest-pool-workers with D1 + R2 fixtures"
```

---

### Task 10: `GET /api/search` Pages Function

**Files:**
- Create: `apps/web/functions/api/search.ts`
- Create: `apps/web/tests/api-search.test.ts`

**Interfaces:**
- Consumes: `env.DB`, `packages/core` (`expandQuery`, `buildFtsQuery`, `normalizeQuery`), `packages/synonyms`
- Produces: `SearchResponse` (from `@icon-collection/core`). shape 変更なし
- 400: `q` 空 / `limit` > 200
- 500: D1 error

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// apps/web/tests/api-search.test.ts
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test';
import { describe, expect, it, beforeEach } from 'vitest';
import { seedTestDb } from './setup/miniflare.ts';
import { onRequest } from '../functions/api/search.ts';

const call = async (url: string): Promise<Response> => {
  const req = new Request(`https://x${url}`);
  const ctx = createExecutionContext();
  const res = await onRequest({ request: req, env, params: {}, next: async () => new Response(), data: {}, waitUntil: ctx.waitUntil, passThroughOnException: () => undefined, functionPath: '/api/search' } as never);
  await waitOnExecutionContext(ctx);
  return res;
};

beforeEach(() => seedTestDb());

describe('GET /api/search', () => {
  it('returns matching hits with width/height from collection_meta', async () => {
    const res = await call('/api/search?q=home');
    expect(res.status).toBe(200);
    const body = (await res.json()) as { hits: unknown[]; total: number; cursor: string | null };
    expect(body.total).toBe(2);
    expect(body.hits).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ collection: 'mdi', name: 'home', width: 24, height: 24 }),
        expect.objectContaining({ collection: 'lucide', name: 'home', width: 24, height: 24 }),
      ]),
    );
  });

  it('expands query via synonyms (house → home)', async () => {
    const res = await call('/api/search?q=house');
    const body = (await res.json()) as { total: number };
    expect(body.total).toBeGreaterThanOrEqual(1);
  });

  it('filters by collection', async () => {
    const res = await call('/api/search?q=home&collection=mdi');
    const body = (await res.json()) as { hits: Array<{ collection: string }>; total: number };
    expect(body.total).toBe(1);
    expect(body.hits[0]!.collection).toBe('mdi');
  });

  it('returns 400 when q is missing', async () => {
    const res = await call('/api/search');
    expect(res.status).toBe(400);
  });

  it('returns 400 when limit exceeds 200', async () => {
    const res = await call('/api/search?q=home&limit=201');
    expect(res.status).toBe(400);
  });

  it('returns cursor for further pagination when total > limit', async () => {
    // extra 100 rows for pagination test omitted; verify shape only
    const res = await call('/api/search?q=home&limit=1');
    const body = (await res.json()) as { cursor: string | null; total: number };
    expect(body.total).toBe(2);
    expect(typeof body.cursor).toBe('string');
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/web test -- tests/api-search.test.ts`
Expected: fail — function 未定義。

- [ ] **Step 3: 実装**

```typescript
// apps/web/functions/api/search.ts
import type { IconHit, SearchResponse } from '@icon-collection/core';
import { buildFtsQuery, expandQuery, normalizeQuery } from '@icon-collection/core';
import { loadDictionary } from '@icon-collection/synonyms';

const DICTS = [loadDictionary('en'), loadDictionary('ja')] as const;

type Env = { DB: D1Database };
type PagesFunction = (ctx: {
  request: Request;
  env: Env;
  params: Record<string, string | string[]>;
}) => Promise<Response>;

const MAX_LIMIT = 200;
const DEFAULT_LIMIT = 60;

const decodeCursor = (raw: string | null): number => {
  if (!raw) return 0;
  try {
    const { offset } = JSON.parse(atob(raw.replace(/-/g, '+').replace(/_/g, '/'))) as { offset: number };
    return Number.isFinite(offset) && offset >= 0 ? offset : 0;
  } catch {
    return 0;
  }
};

const encodeCursor = (offset: number): string =>
  btoa(JSON.stringify({ offset })).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');

const jsonError = (status: number, message: string): Response =>
  new Response(JSON.stringify({ error: message }), {
    status,
    headers: { 'content-type': 'application/json' },
  });

export const onRequest: PagesFunction = async ({ request, env }) => {
  const url = new URL(request.url);
  const q = url.searchParams.get('q');
  if (!q || normalizeQuery(q).length === 0) return jsonError(400, 'q is required');
  const limitRaw = url.searchParams.get('limit');
  const limit = limitRaw ? Number.parseInt(limitRaw, 10) : DEFAULT_LIMIT;
  if (!Number.isFinite(limit) || limit <= 0 || limit > MAX_LIMIT) {
    return jsonError(400, 'limit must be 1..200');
  }
  const offset = decodeCursor(url.searchParams.get('cursor'));
  const collections = url.searchParams.getAll('collection');
  const licenses = url.searchParams.getAll('license');

  const terms = expandQuery(q, DICTS);
  const fts = buildFtsQuery(terms);
  if (fts.length === 0) {
    return Response.json({ hits: [], total: 0, cursor: null } satisfies SearchResponse, {
      headers: { 'cache-control': 's-maxage=60, stale-while-revalidate=300' },
    });
  }

  const conds: string[] = ['icons_fts MATCH ?'];
  const params: unknown[] = [fts];
  if (collections.length > 0) {
    conds.push(`i.collection IN (${collections.map(() => '?').join(', ')})`);
    params.push(...collections);
  }
  if (licenses.length > 0) {
    conds.push(`i.license IN (${licenses.map(() => '?').join(', ')})`);
    params.push(...licenses);
  }
  const where = conds.join(' AND ');

  const countSql = `SELECT COUNT(*) AS n FROM icons i JOIN icons_fts f ON f.rowid = i.id WHERE ${where}`;
  const listSql = `SELECT i.collection, i.name, i.license FROM icons i JOIN icons_fts f ON f.rowid = i.id WHERE ${where} ORDER BY bm25(icons_fts) LIMIT ? OFFSET ?`;

  try {
    const countRow = await env.DB.prepare(countSql).bind(...params).first<{ n: number }>();
    const rowsRes = await env.DB.prepare(listSql).bind(...params, limit, offset).all<{ collection: string; name: string; license: string }>();
    const rows = rowsRes.results ?? [];
    const total = countRow?.n ?? 0;
    const usedCollections = Array.from(new Set(rows.map((r) => r.collection)));
    let dims = new Map<string, { w: number; h: number }>();
    if (usedCollections.length > 0) {
      const metaSql = `SELECT collection, default_width, default_height FROM collection_meta WHERE collection IN (${usedCollections.map(() => '?').join(', ')})`;
      const metaRes = await env.DB.prepare(metaSql).bind(...usedCollections).all<{ collection: string; default_width: number; default_height: number }>();
      dims = new Map(
        (metaRes.results ?? []).map((r) => [r.collection, { w: r.default_width, h: r.default_height }]),
      );
    }
    const hits: IconHit[] = rows.map((r) => {
      const d = dims.get(r.collection) ?? { w: 24, h: 24 };
      return { collection: r.collection, name: r.name, license: r.license, width: d.w, height: d.h };
    });
    const nextOffset = offset + hits.length;
    const cursor = nextOffset < total ? encodeCursor(nextOffset) : null;
    return Response.json({ hits, total, cursor } satisfies SearchResponse, {
      headers: { 'cache-control': 's-maxage=60, stale-while-revalidate=300' },
    });
  } catch (e) {
    console.error(JSON.stringify({ path: '/api/search', error: e instanceof Error ? e.message : String(e) }));
    return jsonError(500, 'internal');
  }
};
```

- [ ] **Step 4: テスト通過確認**

Run: `pnpm -F @icon-collection/web test`
Expected: PASS 全件。

- [ ] **Step 5: commit**

```bash
git add apps/web
git commit -m "feat(web): implement /api/search with FTS5 and synonym expansion"
```

---

### Task 11: `GET /icon/{collection}/{name}.svg` Pages Function

**Files:**
- Create: `apps/web/functions/icon/[collection]/[name].svg.ts`
- Create: `apps/web/tests/icon-svg.test.ts`
- Create: `apps/web/src/lib/iconify-cache.ts` — Iconify JSON を R2 から取ってメモリキャッシュするヘルパ

**Interfaces:**
- Consumes: `env.ICONS` (R2), Cache API (`caches.default`)
- Produces: `image/svg+xml` with `cache-control: public, max-age=31536000, immutable`, `etag: "sha256:<hash8>"`
- 404 if collection JSON absent or icon body missing
- 500 on unsafe SVG detection (`<script`, ` on[a-z]+=`, `<foreignObject`)

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// apps/web/tests/icon-svg.test.ts
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test';
import { beforeEach, describe, expect, it } from 'vitest';
import { onRequest } from '../functions/icon/[collection]/[name].svg.ts';
import { putIconFixture } from './setup/miniflare.ts';

const FIX = {
  prefix: 'mdi',
  info: { name: 'MDI' },
  width: 24,
  height: 24,
  icons: { home: { body: '<path d="M0 0h24v24H0z"/>' } },
};

const call = async (collection: string, name: string): Promise<Response> => {
  const ctx = createExecutionContext();
  const res = await onRequest({
    request: new Request(`https://x/icon/${collection}/${name}.svg`),
    env, params: { collection, name },
    next: async () => new Response(), data: {},
    waitUntil: ctx.waitUntil, passThroughOnException: () => undefined,
    functionPath: `/icon/${collection}/${name}.svg`,
  } as never);
  await waitOnExecutionContext(ctx);
  return res;
};

beforeEach(async () => {
  await putIconFixture('mdi', FIX);
});

it('returns valid SVG with immutable cache headers', async () => {
  const res = await call('mdi', 'home');
  expect(res.status).toBe(200);
  expect(res.headers.get('content-type')).toContain('image/svg+xml');
  expect(res.headers.get('cache-control')).toBe('public, max-age=31536000, immutable');
  expect(res.headers.get('etag')).toMatch(/^"sha256:[a-f0-9]{8}"$/);
  const body = await res.text();
  expect(body).toContain('viewBox="0 0 24 24"');
  expect(body).toContain('xmlns="http://www.w3.org/2000/svg"');
  expect(body).toContain('<path d="M0 0h24v24H0z"/>');
});

it('returns 404 when icon missing', async () => {
  const res = await call('mdi', 'unknown-icon');
  expect(res.status).toBe(404);
});

it('returns 500 when body contains <script>', async () => {
  await putIconFixture('bad', {
    prefix: 'bad', info: { name: 'Bad' }, width: 24, height: 24,
    icons: { evil: { body: '<script>alert(1)</script>' } },
  });
  const res = await call('bad', 'evil');
  expect(res.status).toBe(500);
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/web test -- tests/icon-svg.test.ts`
Expected: fail — function 未定義。

- [ ] **Step 3: helper 実装**

```typescript
// apps/web/src/lib/iconify-cache.ts
import type { IconifyJSON } from '@iconify/types';

const memory = new Map<string, IconifyJSON>();

export const loadCollection = async (env: { ICONS: R2Bucket }, collection: string): Promise<IconifyJSON | null> => {
  const cached = memory.get(collection);
  if (cached) return cached;
  const obj = await env.ICONS.get(`iconify/${collection}.json`);
  if (!obj) return null;
  const json = (await obj.json()) as IconifyJSON;
  memory.set(collection, json);
  return json;
};

const UNSAFE = /<script\b|<foreignObject\b|\son[a-z]+\s*=/i;
export const isUnsafeSvg = (body: string): boolean => UNSAFE.test(body);

export const hashSha256 = async (input: string): Promise<string> => {
  const buf = new TextEncoder().encode(input);
  const digest = await crypto.subtle.digest('SHA-256', buf);
  const hex = Array.from(new Uint8Array(digest)).map((b) => b.toString(16).padStart(2, '0')).join('');
  return hex.slice(0, 8);
};
```

- [ ] **Step 4: function 実装**

```typescript
// apps/web/functions/icon/[collection]/[name].svg.ts
import { hashSha256, isUnsafeSvg, loadCollection } from '../../../src/lib/iconify-cache.ts';

type Env = { ICONS: R2Bucket };
type Ctx = { request: Request; env: Env; params: { collection: string; name: string } };

export const onRequest = async ({ request, env, params }: Ctx): Promise<Response> => {
  const cache = caches.default;
  const cached = await cache.match(request);
  if (cached) return cached;

  const collection = params.collection;
  const name = params.name;
  const json = await loadCollection(env, collection);
  if (!json) return new Response('not found', { status: 404 });
  const icon = json.icons?.[name];
  if (!icon || typeof icon.body !== 'string') return new Response('not found', { status: 404 });

  if (isUnsafeSvg(icon.body)) return new Response('unsafe content', { status: 500 });

  const w = icon.width ?? json.width ?? 24;
  const h = icon.height ?? json.height ?? 24;
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${w} ${h}">${icon.body}</svg>`;
  const etag = `"sha256:${await hashSha256(svg)}"`;
  const res = new Response(svg, {
    headers: {
      'content-type': 'image/svg+xml; charset=utf-8',
      'cache-control': 'public, max-age=31536000, immutable',
      etag,
    },
  });
  await cache.put(request, res.clone());
  return res;
};
```

- [ ] **Step 5: テスト通過確認**

Run: `pnpm -F @icon-collection/web test`
Expected: PASS 全件。

- [ ] **Step 6: commit**

```bash
git add apps/web
git commit -m "feat(web): serve /icon/{collection}/{name}.svg from R2"
```

---

### Task 12: `GET /icon/{collection}/{name}.mx` Pages Function

**Files:**
- Create: `apps/web/functions/icon/[collection]/[name].mx.ts`
- Create: `apps/web/tests/icon-mx.test.ts`

**Interfaces:**
- Consumes: 同 SVG endpoint + `svgToMxLibrary` (`@icon-collection/ui`)
- Produces: `application/xml; charset=utf-8`, 同 cache headers

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// apps/web/tests/icon-mx.test.ts
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test';
import { beforeEach, describe, expect, it } from 'vitest';
import { onRequest } from '../functions/icon/[collection]/[name].mx.ts';
import { putIconFixture } from './setup/miniflare.ts';

const FIX = { prefix: 'mdi', info: { name: 'MDI' }, width: 24, height: 24, icons: { home: { body: '<path d="M0 0h24v24H0z"/>' } } };

const call = async (collection: string, name: string): Promise<Response> => {
  const ctx = createExecutionContext();
  const res = await onRequest({
    request: new Request(`https://x/icon/${collection}/${name}.mx`),
    env, params: { collection, name },
    next: async () => new Response(), data: {},
    waitUntil: ctx.waitUntil, passThroughOnException: () => undefined,
    functionPath: `/icon/${collection}/${name}.mx`,
  } as never);
  await waitOnExecutionContext(ctx);
  return res;
};

beforeEach(async () => { await putIconFixture('mdi', FIX); });

it('returns mxLibrary XML wrapping the SVG', async () => {
  const res = await call('mdi', 'home');
  expect(res.status).toBe(200);
  expect(res.headers.get('content-type')).toContain('application/xml');
  const body = await res.text();
  expect(body).toContain('<mxGraphModel>');
  expect(body).toContain('data:image/svg+xml,');
});

it('returns 404 when icon missing', async () => {
  const res = await call('mdi', 'unknown-icon');
  expect(res.status).toBe(404);
});
```

- [ ] **Step 2: 失敗を確認 → 実装**

```typescript
// apps/web/functions/icon/[collection]/[name].mx.ts
import { svgToMxLibrary } from '@icon-collection/ui';
import { hashSha256, isUnsafeSvg, loadCollection } from '../../../src/lib/iconify-cache.ts';

type Env = { ICONS: R2Bucket };
type Ctx = { request: Request; env: Env; params: { collection: string; name: string } };

export const onRequest = async ({ request, env, params }: Ctx): Promise<Response> => {
  const cache = caches.default;
  const cached = await cache.match(request);
  if (cached) return cached;

  const json = await loadCollection(env, params.collection);
  if (!json) return new Response('not found', { status: 404 });
  const icon = json.icons?.[params.name];
  if (!icon || typeof icon.body !== 'string') return new Response('not found', { status: 404 });
  if (isUnsafeSvg(icon.body)) return new Response('unsafe content', { status: 500 });

  const w = icon.width ?? json.width ?? 24;
  const h = icon.height ?? json.height ?? 24;
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${w} ${h}">${icon.body}</svg>`;
  const mx = svgToMxLibrary(svg);
  const etag = `"sha256:${await hashSha256(mx)}"`;
  const res = new Response(mx, {
    headers: {
      'content-type': 'application/xml; charset=utf-8',
      'cache-control': 'public, max-age=31536000, immutable',
      etag,
    },
  });
  await cache.put(request, res.clone());
  return res;
};
```

- [ ] **Step 3: テスト通過確認**

Run: `pnpm -F @icon-collection/web test`
Expected: PASS 全件。

- [ ] **Step 4: commit**

```bash
git add apps/web
git commit -m "feat(web): serve /icon/{collection}/{name}.mx mxlibrary payload"
```

---

### Task 13: `SearchPage` island 実装 (SearchBox → useSearch → FilterBar → IconGrid → CopyMenu)

**Files:**
- Modify: `apps/web/src/islands/SearchPage.tsx` — 実装
- Modify: `apps/web/src/pages/index.astro` — props 更新
- Create: `apps/web/tests/search-page.test.tsx`

**Interfaces:**
- Consumes: `packages/ui` (`HostProvider`, `SearchBox`, `FilterBar`, `IconGrid`, `IconCell`, `CopyMenu`, `useSearch`, `useCopy`, `createSvgCache`), `packages/core` (`createApiClient`)
- Host は `apiBaseUrl` = `location.origin` (island hydration 時) を使用
- `showToast`: 画面内固定 toast (10 秒後 fade)
- `persistState`: `localStorage`

- [ ] **Step 1: 失敗するテストを書く**

```tsx
// apps/web/tests/search-page.test.tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/preact';
import userEvent from '@testing-library/user-event';
import { describe, expect, it, vi } from 'vitest';
import { SearchPage } from '../src/islands/SearchPage.tsx';

it('renders results after user types a query', async () => {
  const fetchImpl = vi.fn(async (url: string) => {
    if (url.includes('/api/search')) {
      return new Response(JSON.stringify({ hits: [{ collection: 'mdi', name: 'home', license: 'Apache-2.0', width: 24, height: 24 }], total: 1, cursor: null }));
    }
    return new Response('<svg/>', { headers: { 'content-type': 'image/svg+xml' } });
  });
  globalThis.fetch = fetchImpl as unknown as typeof fetch;
  render(<SearchPage apiBaseUrl="" />);
  await userEvent.type(screen.getByRole('searchbox'), 'home');
  await waitFor(() => expect(screen.getByLabelText('mdi/home')).toBeInTheDocument());
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm -F @icon-collection/web test -- tests/search-page.test.tsx`
Expected: fail (placeholder SearchPage 実装のため)。

- [ ] **Step 3: 実装 (`ToastHost` を先に作る)**

```tsx
// apps/web/src/islands/ToastHost.tsx
import { useEffect, useState } from 'preact/hooks';

let externalPush: ((m: string) => void) | null = null;
export const pushToast = (m: string): void => externalPush?.(m);

export const ToastHost = () => {
  const [items, setItems] = useState<{ id: number; text: string }[]>([]);
  useEffect(() => {
    externalPush = (text) => {
      const id = Date.now() + Math.random();
      setItems((prev) => [...prev, { id, text }]);
      setTimeout(() => setItems((prev) => prev.filter((i) => i.id !== id)), 4000);
    };
    return () => {
      externalPush = null;
    };
  }, []);
  return (
    <div class="pointer-events-none fixed inset-x-0 bottom-4 flex flex-col items-center gap-2">
      {items.map((i) => (
        <div key={i.id} class="pointer-events-auto rounded bg-neutral-900 px-3 py-1.5 text-sm text-white shadow">
          {i.text}
        </div>
      ))}
    </div>
  );
};
```

```tsx
// apps/web/src/islands/SearchPage.tsx
import { createApiClient } from '@icon-collection/core';
import type { ApiClient, SearchQuery } from '@icon-collection/core';
import {
  createSvgCache,
  FilterBar,
  HostProvider,
  IconGrid,
  SearchBox,
  useSearch,
} from '@icon-collection/ui';
import type { FilterValue, Host } from '@icon-collection/ui';
import { useMemo, useState } from 'preact/hooks';
import { pushToast, ToastHost } from './ToastHost.tsx';

export type SearchPageProps = { apiBaseUrl: string };

const useHost = (apiBaseUrl: string): Host =>
  useMemo(() => {
    const base = apiBaseUrl || (typeof location !== 'undefined' ? location.origin : '');
    const apiClient: ApiClient = createApiClient({ baseUrl: base });
    return {
      apiBaseUrl: base,
      apiClient,
      svgCache: createSvgCache(),
      copyText: async (s) => {
        if (typeof navigator !== 'undefined' && navigator.clipboard) {
          await navigator.clipboard.writeText(s);
        }
      },
      showToast: (m) => pushToast(m),
      persistState: {
        get: async (k) => (typeof localStorage !== 'undefined' ? localStorage.getItem(k) : null),
        set: async (k, v) => {
          if (typeof localStorage !== 'undefined') localStorage.setItem(k, v);
        },
      },
    };
  }, [apiBaseUrl]);

const COLLECTION_OPTIONS = [
  { name: 'mdi', label: 'MDI' },
  { name: 'lucide', label: 'Lucide' },
  { name: 'heroicons', label: 'Heroicons' },
  { name: 'tabler', label: 'Tabler' },
] as const;

const LICENSE_OPTIONS = ['Apache-2.0', 'MIT', 'ISC', 'CC-BY-4.0'] as const;

const SearchInner = () => {
  const [q, setQ] = useState('');
  const [filter, setFilter] = useState<FilterValue>({ collection: [], license: [] });
  const query: SearchQuery | null = q.trim()
    ? {
        q,
        ...(filter.collection.length > 0 ? { collection: filter.collection } : {}),
        ...(filter.license.length > 0 ? { license: filter.license } : {}),
        limit: 60,
      }
    : null;
  const state = useSearch(query);
  return (
    <div class="flex flex-col gap-4">
      <SearchBox initialValue={q} onChange={setQ} placeholder="Search icons…" />
      <FilterBar
        collections={COLLECTION_OPTIONS}
        licenses={LICENSE_OPTIONS}
        value={filter}
        onChange={setFilter}
      />
      {state.status === 'error' ? (
        <p class="text-red-600">Error: {state.error?.message}</p>
      ) : (
        <IconGrid hits={state.data?.hits ?? []} />
      )}
    </div>
  );
};

export const SearchPage = ({ apiBaseUrl }: SearchPageProps) => {
  const host = useHost(apiBaseUrl);
  return (
    <HostProvider host={host}>
      <SearchInner />
      <ToastHost />
    </HostProvider>
  );
};
```

- [ ] **Step 4: index.astro 更新**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SearchPage } from '../islands/SearchPage.tsx';
---
<BaseLayout title="IconCollection">
  <main class="mx-auto flex max-w-6xl flex-col gap-6 p-6">
    <h1 class="text-2xl font-semibold">IconCollection</h1>
    <SearchPage client:load apiBaseUrl="" />
  </main>
</BaseLayout>
```

- [ ] **Step 5: 依存の確認 (`@icon-collection/synonyms` は不要、client-side は core の expand を叩かない — expand はサーバ側で行う)**

- [ ] **Step 6: build 通過確認**

Run: `pnpm -F @icon-collection/web build && pnpm -F @icon-collection/web test`
Expected: PASS。

- [ ] **Step 7: commit**

```bash
git add apps/web
git commit -m "feat(web): implement search page island with HostProvider"
```

---

### Task 14: セキュリティヘッダ (CSP) middleware

**Files:**
- Create: `apps/web/functions/_middleware.ts`
- Create: `apps/web/tests/middleware-headers.test.ts`

**Interfaces:**
- 全レスポンスに `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin` を追加
- `/icon/*` は `content-type: image/svg+xml` / `application/xml` を維持 (middleware は Response header にのみ介入、body 触らない)

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// apps/web/tests/middleware-headers.test.ts
import { env, createExecutionContext, waitOnExecutionContext } from 'cloudflare:test';
import { describe, expect, it } from 'vitest';
import { onRequest as middleware } from '../functions/_middleware.ts';

it('adds CSP and X-Content-Type-Options headers to next response', async () => {
  const nextRes = new Response('ok');
  const ctx = createExecutionContext();
  const res = await middleware({
    request: new Request('https://x/'), env,
    params: {}, data: {},
    next: async () => nextRes,
    waitUntil: ctx.waitUntil, passThroughOnException: () => undefined,
    functionPath: '/',
  } as never);
  await waitOnExecutionContext(ctx);
  expect(res.headers.get('content-security-policy')).toContain("default-src 'self'");
  expect(res.headers.get('x-content-type-options')).toBe('nosniff');
  expect(res.headers.get('referrer-policy')).toBe('strict-origin-when-cross-origin');
});
```

- [ ] **Step 2: 実装**

```typescript
// apps/web/functions/_middleware.ts
type Ctx = { next: () => Promise<Response> };

const CSP = [
  "default-src 'self'",
  "img-src 'self' data:",
  "style-src 'self' 'unsafe-inline'",
  "script-src 'self'",
  "connect-src 'self'",
  "frame-ancestors 'none'",
].join('; ');

export const onRequest = async ({ next }: Ctx): Promise<Response> => {
  const res = await next();
  const headers = new Headers(res.headers);
  headers.set('content-security-policy', CSP);
  headers.set('x-content-type-options', 'nosniff');
  headers.set('referrer-policy', 'strict-origin-when-cross-origin');
  return new Response(res.body, { status: res.status, statusText: res.statusText, headers });
};
```

- [ ] **Step 3: テスト**

Run: `pnpm -F @icon-collection/web test`
Expected: PASS。

- [ ] **Step 4: commit**

```bash
git add apps/web/functions/_middleware.ts apps/web/tests/middleware-headers.test.ts
git commit -m "feat(web): add security headers middleware"
```

---

### Task 15: `apps/web/README.md`

**Files:**
- Create: `apps/web/README.md`

**Interfaces:** none (docs only)

- [ ] **Step 1: 執筆**

```markdown
# @icon-collection/web

Cloudflare Pages で配信する検索 UI と icon delivery API。

## Runtime

- Astro 5 (SSG) + Preact islands + Tailwind CSS v4
- Cloudflare Pages Functions (`functions/**`)
  - `GET /api/search`
  - `GET /icon/{collection}/{name}.svg`
  - `GET /icon/{collection}/{name}.mx`
- Bindings: `DB` (D1, `icon-collection`) / `ICONS` (R2, `icon-collection`)

## Local development

1. `pnpm -F @icon-collection/web install`
2. `cp apps/web/.dev.vars.example apps/web/.dev.vars` (未設定でも SSG は動く)
3. `pnpm -F @icon-collection/web dev` — Astro dev server (Functions は fetch mock 未接続、API テストは `pnpm test` を使用)
4. `pnpm -F @icon-collection/web test` — vitest-pool-workers による Functions 統合テスト
5. `pnpm -F @icon-collection/web build` — `dist/` を生成
6. `pnpm -F @icon-collection/web preview` — `wrangler pages dev ./dist` で bindings 込みプレビュー

## Deploy

Cloudflare Pages の Git 連携 (`master` push → auto deploy)。

Pages プロジェクト設定:
- Build command: `pnpm -F @icon-collection/web build`
- Output directory: `apps/web/dist`
- Root directory: `/` (monorepo root)
- Environment variables: なし (bindings 経由)
- Bindings: `DB` (D1), `ICONS` (R2) をダッシュボードから登録

## API contract

`GET /api/search?q=<string>&collection=<repeat>&license=<repeat>&limit=<1-200>&cursor=<opaque>`

Response 200 (JSON):

    { "hits": [{"collection":"mdi","name":"home","license":"Apache-2.0","width":24,"height":24}],
      "total": 123,
      "cursor": "eyJvZmZzZXQiOjYwfQ" }

## Cache policy

- `/icon/*` — `public, max-age=31536000, immutable`
- `/api/search` — `s-maxage=60, stale-while-revalidate=300`
- 全レスポンスに CSP / nosniff / referrer-policy

## Security

- SVG body は `<script>` / event handler / `<foreignObject>` を検出したら 500
- CSP `default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self'; frame-ancestors 'none'`
```

- [ ] **Step 2: commit**

```bash
git add apps/web/README.md
git commit -m "docs(web): describe API contract, deploy, and cache policy"
```

---

### Task 16: CI: `apps/web` の typecheck / test / build を追加

**Files:**
- Modify: `.github/workflows/ci.yml` (存在すれば) or Create: `.github/workflows/ci.yml`

**Interfaces:** none

- [ ] **Step 1: 現状の CI を確認**

Run: `ls .github/workflows`
- 既存 workflow があれば `pnpm typecheck && pnpm test && pnpm -F @icon-collection/web build` を追加。
- なければ最小の CI を新規作成:

```yaml
# .github/workflows/ci.yml
name: ci
on:
  push:
    branches: [master, "feat/**", "fix/**"]
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm -F @icon-collection/web build
```

- [ ] **Step 2: ローカルで最終確認**

Run: `pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm -F @icon-collection/web build`
Expected: 全 workspace green。

- [ ] **Step 3: commit + push**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: run lint/typecheck/test/build on every push and PR"
git push -u origin feat/revamp-web
```

- [ ] **Step 4: PR 作成**

Run: `gh pr create --title "feat: web app + icon delivery API" --body "$(cat <<'EOF'
## Summary
- Astro + Preact + Tailwind v4 + Pages Functions で apps/web を新規実装
- /api/search, /icon/*.svg, /icon/*.mx を実装
- Plan A/B の deferred items をすべて解消 (splitStatements 抽出 / batchAtomic 原子化 / collection_meta 拡張 / svgCache スコープ化 / ApiClient 注入 / useCopy エラー surface)

## Test plan
- [ ] pnpm test — 全 workspace PASS
- [ ] pnpm typecheck — clean
- [ ] pnpm -F @icon-collection/web build — dist/ が生成される
- [ ] Cloudflare Pages preview で /api/search と /icon/mdi/home.svg が 200 を返す
EOF
)"`

---

## Post-Plan Follow-ups (別 plan)

- Plan D: VSCode 拡張 (tsup + Preact WebView, OIDC publish)
- Plan E: 旧配信経路の停止 (DNS 切替 / 後方互換 route / 旧 asset 削除)
- Plan B': React Icons を独立コレクションとして追加
