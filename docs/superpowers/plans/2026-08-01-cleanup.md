# Cleanup + Legacy Removal Implementation Plan (Plan E)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** revamp イニシアチブの最終段。旧アセット (`extension/`, `parsers/`) 削除 + 旧 URL 後方互換 redirect + Plan C/D の Minor follow-up 一括解消。

**Architecture:** apps/web に `_redirects` を追加し `/{c}/{n}.svg` を新 `/icon/{c}/{n}.svg` に 301。VSCode 拡張の Tailwind pipeline を配線。web/ingest の未使用/重複コード除去。

**Tech Stack:** 既存スタック維持。追加: `@tailwindcss/cli` (VSCode extension pre-build 用)

## Global Constraints

- Node 22 LTS / pnpm 9 / TypeScript strict (全フラグ)
- Vitest 4 / Biome 2 / tsup / Astro
- 依存追加は `pnpm add` 経由、ハードコード禁止
- linter-ignore directive 禁止
- git rm で削除履歴を残す (旧 `extension/` + `parsers/`)
- 既存テストを壊さない (behavioral 変更を伴うタスクは TDD で対応)
- `_redirects` は `apps/web/public/` 直下に配置 (Astro/Cloudflare adapter が dist に copy)

---

### Task 1: 旧 URL スキーム後方互換 `_redirects`

**Files:**
- Create: `apps/web/public/_redirects`
- Create: `apps/web/tests/redirects.test.ts` (Pages Functions 経路ではないため文字列 assertion のみで OK)

**Interfaces:**
- Cloudflare Pages が build 出力の `_redirects` を認識、edge で 301 を返す。Pages Functions 発火なし

- [ ] **Step 1: `apps/web/public/_redirects` 作成**

```
/:collection/:name.svg /icon/:collection/:name.svg 301
/:collection/:name.mx /icon/:collection/:name.mx 301
```

- [ ] **Step 2: `_redirects` が `dist` に copy されることを検証**

```bash
pnpm -F @icon-collection/web build
ls apps/web/dist/_redirects
```

Expected: file exists, content matches source (Astro / Cloudflare adapter が `public/` の内容を dist root に copy する既定動作)

- [ ] **Step 3: file content assertion テストを書く**

```typescript
// apps/web/tests/redirects.test.ts
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { describe, expect, it } from 'vitest';

const redirectsPath = fileURLToPath(new URL('../public/_redirects', import.meta.url));

describe('_redirects', () => {
  it('redirects legacy SVG URLs to /icon path', async () => {
    const content = await readFile(redirectsPath, 'utf-8');
    expect(content).toContain('/:collection/:name.svg /icon/:collection/:name.svg 301');
  });

  it('redirects legacy MX URLs to /icon path', async () => {
    const content = await readFile(redirectsPath, 'utf-8');
    expect(content).toContain('/:collection/:name.mx /icon/:collection/:name.mx 301');
  });

  it('has no wildcard that could catch /api/*', async () => {
    const content = await readFile(redirectsPath, 'utf-8');
    // Each redirect must have a specific extension suffix
    const lines = content.split('\n').filter((l) => l.trim() && !l.startsWith('#'));
    for (const line of lines) {
      const [from] = line.split(/\s+/);
      expect(from).toMatch(/\.(svg|mx)$/);
    }
  });
});
```

このテストは browser project でも workers project でも動く (`fs.readFile` は Node なので browser project に寄せる)。`apps/web/tests/browser/redirects.test.ts` に配置する。

- [ ] **Step 4: `tests/browser/redirects.test.ts` にリネームして配置**

- [ ] **Step 5: テスト実行 → green**

Run: `pnpm -F @icon-collection/web test`
Expected: 15/15 pass (14 prior + 3 new redirect asserts wrapped in 1 file → 3 tests、合計 17 かもしれない。実測に従う)

- [ ] **Step 6: typecheck / lint clean**

- [ ] **Step 7: commit**

```bash
git add apps/web/public/_redirects apps/web/tests/browser/redirects.test.ts
git commit -m "feat(web): add 301 redirects for legacy icon URL scheme"
```

---

### Task 2: 旧 `extension/` と `parsers/` を削除

**Files:**
- Delete: `extension/` (全ファイル)
- Delete: `parsers/` (全ファイル)
- Modify: `biome.json` — `!**/extension` および `!**/parsers` の ignore エントリを削除 (不要になる)

**Interfaces:** none

- [ ] **Step 1: git rm 実行**

```bash
git rm -r extension parsers
```

- [ ] **Step 2: `biome.json` の `files.includes` から関連 ignore を除去**

```jsonc
// biome.json の files.includes を変更
{
  "files": {
    "includes": [
      "**",
      "!**/dist",
      "!**/node_modules",
      "!**/.astro",
      "!**/.wrangler",
      "!**/coverage",
      "!apps/vscode-extension/images",
      "!apps/vscode-extension/.vscode-test"
      // "!**/parsers" 削除
      // "!**/extension" 削除
    ]
  }
}
```

- [ ] **Step 3: `pnpm install` 再実行 (pnpm-workspace.yaml から extension/parsers を暗黙除外できる) `pnpm-workspace.yaml` の変更は不要 (glob は既存パッケージだけを拾う)**

Run: `pnpm install --frozen-lockfile`
Expected: 差分なし (`parsers/package.json` を除外しても他 workspace に影響なし)。もし lockfile diff が出たら再生成: `pnpm install` (--frozen-lockfile なしで) して差分を確認、必要なら commit。

- [ ] **Step 4: 全 workspace で clean を確認**

Run: `pnpm lint && pnpm typecheck && pnpm test`
Expected: 全 green。extension/parsers を参照するコードは Plan B/C/D で完全に置換済みのはず。もし参照が残ってれば grep で場所特定 → fix。

- [ ] **Step 5: commit**

```bash
git add biome.json pnpm-lock.yaml
git commit -m "chore: remove legacy extension/ and parsers/ directories"
```

`git rm` の削除は自動的にステージ済み。上記 `git add` は biome.json と (もし変更されていれば) lockfile のみ。

---

### Task 3: root `README.md` 刷新

**Files:**
- Modify: `README.md` (root)

**Interfaces:** none (docs)

- [ ] **Step 1: 現状 README.md を確認**

```bash
head -60 README.md
```

- [ ] **Step 2: 新 README 内容を書く**

置換する主なポイント:
- 旧 Algolia 記述の削除
- monorepo 構成 (`apps/{web,vscode-extension}`, `packages/{core,synonyms,ui}`, `tools/ingest`) の概要
- 主要 URL (`https://icons.kage1020.com/api/search`, `/icon/{c}/{n}.svg`, `/icon/{c}/{n}.mx`)
- 旧 `/{c}/{n}.svg` は 301 で新 URL にリダイレクトされる旨
- ローカル開発の基本フロー (`pnpm install`, `pnpm -F @icon-collection/web dev` 等)
- テスト / lint / build / package コマンド一覧
- ライセンス継続 (LICENCE ファイル参照)

- [ ] **Step 3: DNS 切替手順の位置**

README には触れず、`docs/dns-cutover.md` に切り出す。dashboard 操作なので code-side action はゼロ、参照 URL とチェックリストのみ。

- [ ] **Step 4: `docs/dns-cutover.md` 作成 (簡潔に)**

```markdown
# DNS Cutover 手順

`icons.kage1020.com` を旧 origin から Cloudflare Pages (`icon-collection-web.pages.dev`) に切り替える。

## 前提
- Cloudflare Pages プロジェクト `icon-collection-web` に `feat/revamp-web` マージ後の master が deploy 済み
- 旧 origin (S3 / GitHub Pages / whatever) の DNS 権威が Cloudflare 側にある

## 手順
1. Cloudflare dashboard → Zone `kage1020.com` → DNS → `icons` レコードの CNAME 先を `icon-collection-web.pages.dev` に変更
2. TTL は 300 (5 分) で先出し、問題なければ 3600 に戻す
3. `curl -I https://icons.kage1020.com/api/search?q=home` で 200 応答確認
4. `curl -I https://icons.kage1020.com/mdi/home.svg` で 301 → `/icon/mdi/home.svg` 確認
5. 旧 origin の Bucket / Pages を 30 日間保持 (rollback 用)、以降 archive

## Rollback
- DNS を旧 origin に戻すだけ。数分で反映
```

- [ ] **Step 5: commit**

```bash
git add README.md docs/dns-cutover.md
git commit -m "docs: refresh README for post-revamp monorepo structure"
```

---

### Task 4: `apps/vscode-extension` Tailwind pipeline

**Files:**
- Modify: `apps/vscode-extension/package.json` — script 追加、`@tailwindcss/cli` を devDependencies に追加
- Modify: `apps/vscode-extension/tsup.config.ts` — CSS loader の `copy` を維持しつつ、tsup 実行前に `tailwindcss` が既に処理した `dist/webview/main.css` が上書きされないよう順序調整
- Create: `apps/vscode-extension/postcss.config.mjs` (Tailwind v4 の CLI が必要とすれば)

**Interfaces:**
- `dist/webview/main.css` に Tailwind の utility classes が展開されたビルド結果が出力される

- [ ] **Step 1: 依存追加**

```bash
pnpm -F icon-collection add -D @tailwindcss/cli
```

- [ ] **Step 2: `package.json` の scripts を書き換え**

```json
"scripts": {
  "build:css": "tailwindcss -i src/webview/main.css -o dist/webview/main.css --minify",
  "build": "tsup && pnpm run build:css",
  "dev": "tsup --watch",
  ...
}
```

Rationale: tsup が先に dist を clean + tsup 出力を書き出す。その後 `build:css` が Tailwind CLI で `main.css` を上書き。順序が逆だと tsup の clean が Tailwind 出力を消す。tsup の `clean: true` は entry 1 (extension) だけ発火するはず (webview entry では別 outDir) だが、安全側でこの順序に。

- [ ] **Step 3: tsup config の CSS loader を再検討**

現状 `loader: { '.css': 'copy' }` は `main.css` を dist に copy する。Tailwind CLI が同じパスに書き出すので、tsup の copy 出力は上書きされる。挙動としては問題ないが、tsup 側から `main.css` を出力すること自体をやめる方が明快:

```typescript
// tsup.config.ts webview entry の loader 設定を削除、代わりに main.css の import を tsup entry から除外する仕組みは無い
// → 保険として loader は維持 (Tailwind 出力が優先されるだけ)
```

このステップでは変更なし。次ステップの build 検証で問題が出れば戻る。

- [ ] **Step 4: build 実行**

```bash
pnpm -F icon-collection build
```

Expected:
- `dist/extension.cjs` 生成
- `dist/webview/main.js` 生成
- `dist/webview/main.css` が Tailwind 処理後のもの (少なくとも数 KB 以上、`@import "tailwindcss"` が展開されている)
- 元の 65 バイト状態でないこと

- [ ] **Step 5: main.css の内容を目視確認**

```bash
head -20 apps/vscode-extension/dist/webview/main.css
wc -c apps/vscode-extension/dist/webview/main.css
```

Expected: `@import` の literal ではなく、CSS custom properties / utility classes が並んでいる。ファイルサイズ > 10KB。

- [ ] **Step 6: 単体テストは変更不要 (Tailwind pipeline は build 段階のみ)**

Run: `pnpm -F icon-collection test`
Expected: 19/19 変更なし。

- [ ] **Step 7: typecheck / lint clean**

- [ ] **Step 8: commit**

```bash
git add apps/vscode-extension/package.json pnpm-lock.yaml
git commit -m "build(vscode-extension): process Tailwind directives with @tailwindcss/cli"
```

---

### Task 5: sanitizeApiBaseUrl regression テスト + `.vscodeignore` CHANGELOG ペア

**Files:**
- Modify: `apps/vscode-extension/tests/host-adapter.test.ts` (2 テストケース追加)
- Modify: `apps/vscode-extension/.vscodeignore`

**Interfaces:** none

- [ ] **Step 1: regression テスト追加**

```typescript
// apps/vscode-extension/tests/host-adapter.test.ts の sanitizeApiBaseUrl テスト group に追加

it('drops userinfo (user:pass@) from the returned origin', () => {
  expect(sanitizeApiBaseUrl('https://user:pass@example.com')).toBe('https://example.com');
});

it('preserves IPv6 origin', () => {
  expect(sanitizeApiBaseUrl('https://[::1]:8443')).toBe('https://[::1]:8443');
});
```

`sanitizeApiBaseUrl` が exported されていることを確認。もし private なら export 追加 (test 目的の export は許容)。

- [ ] **Step 2: `.vscodeignore` 変更**

現状 (Plan D):
```
.vscode/**
.vscode-test/**
tests/**
...
!dist/**
node_modules/**
.gitignore
.npmignore
```

書き換え:
```
.vscode/**
.vscode-test/**
tests/**
tsup.config.ts
tsconfig*.json
vitest.config.ts
src/**
!dist/**
node_modules/**
.gitignore
.npmignore
CHANGELOG.md
!CHANGELOG.md
```

`CHANGELOG.md` を一度除外して `!CHANGELOG.md` で戻すことで、glob 展開順に依存しない確定的な包含になる。

- [ ] **Step 3: package 実行で `CHANGELOG.md` が vsix に含まれることを確認**

```bash
pnpm -F icon-collection package
# .vsix を tmp で unzip して CHANGELOG.md がいることを確認
```

または vsce の verbose 出力から確認。

- [ ] **Step 4: テスト実行**

Run: `pnpm -F icon-collection test`
Expected: 21/21 pass (19 prior + 2 new)

- [ ] **Step 5: typecheck / lint clean**

- [ ] **Step 6: commit**

```bash
git add apps/vscode-extension/tests/host-adapter.test.ts apps/vscode-extension/.vscodeignore
git commit -m "test(vscode-extension): guard sanitizer against userinfo and IPv6 edge cases"
```

---

### Task 6: `apps/web` 未使用 devDep + deprecated import 移行 + 未使用 env

**Files:**
- Modify: `apps/web/package.json` — `happy-dom` 削除
- Modify: `apps/web/src/env.d.ts` — `ICON_DEFAULT_WIDTH` 削除
- Modify: `apps/web/tests/**/*.ts` — `cloudflare:test` の `env` import を `cloudflare:workers` に置換 (テスト API 互換性を確認)

**Interfaces:** none

- [ ] **Step 1: `happy-dom` を検索して依存が本当にないことを確認**

```bash
grep -rn "happy-dom\|happyDom" apps/web/
```

Expected: `package.json` の devDeps 記述のみ、実コードでは参照なし。もし `vitest.browser.config.ts` の `environment: 'happy-dom'` があれば残す判断 (browser project で必要)。

- [ ] **Step 2: `happy-dom` 削除**

もし Step 1 で `vitest.browser.config.ts` に `environment: 'happy-dom'` があれば、`happy-dom` は browser project の環境として **必要** なので削除しない。この場合 Step 2 スキップ。実測に従う。

削除するなら:
```bash
pnpm -F @icon-collection/web remove happy-dom
```

- [ ] **Step 3: `env.d.ts` から `ICON_DEFAULT_WIDTH` 削除**

```typescript
// apps/web/src/env.d.ts の Cloudflare.Env namespace から ICON_DEFAULT_WIDTH?: string を削除
```

- [ ] **Step 4: `cloudflare:test` → `cloudflare:workers` 置換調査**

`@cloudflare/vitest-pool-workers@0.18.8` のドキュメントを確認。`cloudflare:workers` に `env` があるか否かで対応が異なる。

- (a) `cloudflare:workers` にも `env` があれば直接置換
- (b) `cloudflare:workers` は `env` を持たず `cloudflare:test` の `env` のまま使うのが現行推奨なら、deprecated hint を許容 (TS の hint はコメント + `// @ts-expect-error` は禁止だが、hint は元から throw しない)。この場合何もしない
- (c) API 差異があれば shim を `apps/web/tests/setup/env.ts` に作って包む

まずはドキュメントと `node_modules/@cloudflare/vitest-pool-workers/dist/**/*.d.ts` を読んで判断。判断結果を report に記載。

- [ ] **Step 5: 対応適用**

判断に従って変更を入れる。deprecated hint 解消が (b) の理由で困難なら、`env.d.ts` の下の方に `// eslint`-style コメントではなく、readme に「hint はライブラリの内部事情で今は解消できない」と記す。

- [ ] **Step 6: テスト実行**

Run: `pnpm -F @icon-collection/web test`
Expected: 全テスト green (14/14 or 15/15 depending on Task 1 additions)

- [ ] **Step 7: typecheck / lint clean**

- [ ] **Step 8: commit**

```bash
git add apps/web
git commit -m "chore(web): trim unused devDeps and env fields, refresh test imports"
```

---

### Task 7: `apps/web/functions/api/search.ts` empty expandQuery 応答の cache 見直し

**Files:**
- Modify: `apps/web/functions/api/search.ts`
- Modify: `apps/web/tests/api-search.test.ts` (assertion 追加)

**Interfaces:** none

- [ ] **Step 1: 現状挙動を確認**

`apps/web/functions/api/search.ts` の `if (fts.length === 0)` ブランチが `Response.json(..., { headers: { 'cache-control': 's-maxage=60, stale-while-revalidate=300' } })` を返している行を特定。

- [ ] **Step 2: failing テスト追加**

```typescript
// apps/web/tests/api-search.test.ts に追加
it('does not set shared cache header when expandQuery yields no terms', async () => {
  // q に FTS で expand 不可能な文字列 (e.g. all-symbol) を渡し、buildFtsQuery が空を返すケース
  // Alternatively, mock loadDictionary/expandQuery to return [] — but ingest tests already cover expand()
  // Direct route: 空白のみクエリは normalizeQuery で length 0 になり 400 なので、それは対象外
  // Instead: single-char '?' や句読点だけのクエリで expand が [] を返すのを利用
  const res = await call('/api/search?q=%3F%3F');  // "??" は buildFtsQuery で全部除去され空
  expect(res.status).toBe(200);
  const body = (await res.json()) as { hits: unknown[]; total: number };
  expect(body.total).toBe(0);
  expect(res.headers.get('cache-control')).toBe('no-store');
});
```

`buildFtsQuery` の実装で `FTS5_RESERVED = /["*()]/g` で文字を除去 → 残らないパターンをテスト。`?` は正規表現外なので `?` そのままは残る。より確実なのは `\"` (エスケープされた quote) を渡すこと。実装を読んで確実に empty を返す入力を選ぶ。

- [ ] **Step 3: テスト fail を確認**

Run: `pnpm -F @icon-collection/web test -- tests/api-search.test.ts`
Expected: 新テストが `expect(...).toBe('no-store')` で fail (現状は s-maxage を返す)

- [ ] **Step 4: 実装修正**

```typescript
// apps/web/functions/api/search.ts の該当ブランチ
if (fts.length === 0) {
  return Response.json({ hits: [], total: 0, cursor: null } satisfies SearchResponse, {
    headers: { 'cache-control': 'no-store' },
  });
}
```

- [ ] **Step 5: テスト green**

Run: `pnpm -F @icon-collection/web test`
Expected: 全 pass

- [ ] **Step 6: typecheck / lint**

- [ ] **Step 7: commit**

```bash
git add apps/web/functions/api/search.ts apps/web/tests/api-search.test.ts
git commit -m "fix(web): avoid caching empty-query no-op search responses"
```

---

### Task 8: `tools/ingest/tests/_helpers.ts` に `fakeBatchAtomic` 集約

**Files:**
- Create: `tools/ingest/tests/_helpers.ts`
- Modify: `tools/ingest/tests/run.test.ts`
- Modify: `tools/ingest/tests/seed-icons.test.ts`
- Modify: `tools/ingest/tests/seed-synonyms.test.ts`

**Interfaces:**
- Produces: `export const makeFakeBatchAtomic = (): (stmts: readonly {sql: string; params?: readonly unknown[]}[]) => Promise<D1Result[]>` — bind param 数から `meta.changes` を推定するヘルパ

- [ ] **Step 1: 現状の重複箇所を grep**

```bash
grep -rn "fakeBatchAtomic\|\\([^()]*\\?[^()]*\\)/g" tools/ingest/tests/
```

`fakeBatchAtomic` の実装が 3 テストファイルに copy-paste されている状態を確認。

- [ ] **Step 2: `_helpers.ts` を新設**

```typescript
// tools/ingest/tests/_helpers.ts
import type { D1Result } from '../src/d1.ts';

const rowCountFromSql = (sql: string): number => {
  const matches = sql.match(/\([^()]*\?[^()]*\)/g);
  return matches?.length ?? 0;
};

export const makeFakeBatchAtomic = () =>
  async (stmts: readonly { sql: string; params?: readonly unknown[] }[]): Promise<D1Result[]> =>
    stmts.map((stmt) => ({
      success: true,
      meta: {
        changes: /^\s*INSERT/i.test(stmt.sql) ? rowCountFromSql(stmt.sql) : (stmt.params?.length ?? 1),
        last_row_id: null,
      },
      results: [],
    }));
```

正確なセマンティクスは既存 test の期待に合わせる。Step 1 の grep 結果を突き合わせて `changes` の返し方を pin。

- [ ] **Step 3: 3 テストファイルから copy を除去して import に置換**

各テストで:
```typescript
import { makeFakeBatchAtomic } from './_helpers.ts';

const fakeBatchAtomic = makeFakeBatchAtomic();
```

に置き換え。

- [ ] **Step 4: テスト実行**

Run: `pnpm -F @icon-collection/ingest test`
Expected: 51/51 pass (Task 3 で確認済みの数から変更なし)

- [ ] **Step 5: typecheck / lint clean**

- [ ] **Step 6: commit**

```bash
git add tools/ingest/tests
git commit -m "test(ingest): extract shared fakeBatchAtomic helper"
```

---

### Task 9: `tools/ingest/src/collect.ts` overload 解消

**Files:**
- Modify: `tools/ingest/src/collect.ts`
- Modify: `tools/ingest/src/run.ts`
- Modify: `tools/ingest/tests/collect.test.ts` (もし旧 signature を使っていれば object signature に更新)
- Modify: `tools/ingest/tests/run.test.ts` (deps.collect の mock 署名を更新)

**Interfaces:**
- `collectFromIconify` は object signature 単一に。`(collection: string, version: string)` overload を削除
- `run.ts` は自前で `require.resolve('@iconify/json/json/{collection}.json')` して JSON をロードする `load` を組み立て、`collectFromIconify({ collection, load })` を呼ぶ

- [ ] **Step 1: 現状 overload を確認**

```bash
grep -n "collectFromIconify" tools/ingest/src/ tools/ingest/tests/
```

Plan B Task 2 の作業結果として `collect.ts` に (string, string) と object の 2 overload が存在するはず。

- [ ] **Step 2: `run.ts` の呼び出しを object signature に書き換え**

```typescript
// tools/ingest/src/run.ts の collect 呼び出しを書き換え
import { createRequire } from 'node:module';
import { readFile } from 'node:fs/promises';
import type { IconifyJSON } from '@iconify/types';

const require = createRequire(import.meta.url);

const iconifyLoader = async (collection: string): Promise<IconifyJSON> => {
  const jsonPath = require.resolve(`@iconify/json/json/${collection}.json`);
  const raw = await readFile(jsonPath, 'utf-8');
  return JSON.parse(raw) as IconifyJSON;
};

// 呼び出し側:
snapshots.push(await collect({ collection, load: iconifyLoader }));
```

ただし `version` が引数から消えるので、object signature 側で version をどう決めるか要検討。現状 object 実装は `body.info?.version` から取っているので `run.ts` からの version 引数は不要。

もし `run.ts` の元コードが `deps.collect` を diff 情報から取っていて version を必須にしていたら、`deps.collect` の型定義も同時に更新する必要あり。

- [ ] **Step 3: `collect.ts` から string overload を削除**

```typescript
// tools/ingest/src/collect.ts
type CollectFromIconifyInput = {
  collection: string;
  load: (collection: string) => Promise<IconifyJSON>;
};

export async function collectFromIconify(input: CollectFromIconifyInput): Promise<CollectionSnapshot> {
  const body = await input.load(input.collection);
  const [defaultWidth, defaultHeight] = extractDefaultDimensions(body);
  return {
    collection: input.collection,
    version: body.info?.version ?? '0.0.0',
    license: extractLicense(body),
    total: Object.keys(body.icons).length,
    defaultWidth,
    defaultHeight,
    body,
  };
}

// 旧 collectFromPath は残す (別 API として現役)
```

- [ ] **Step 4: テスト更新 (collect.test.ts / run.test.ts)**

`collectFromIconify('mdi', '2.2.400')` 形式の call site を全て `collectFromIconify({ collection: 'mdi', load: async () => fixtureBody })` に書き換え。

- [ ] **Step 5: run.test.ts の deps.collect mock 更新**

`deps.collect` が signature `(collection: string, currentVersion: string) => Promise<Snapshot>` を持っていた場合、object signature に合わせて mock 更新。

- [ ] **Step 6: 全 workspace test 実行**

Run: `pnpm test`
Expected: 全 workspace green (51/51 ingest 含む)

- [ ] **Step 7: typecheck / lint clean**

- [ ] **Step 8: commit**

```bash
git add tools/ingest
git commit -m "refactor(ingest): unify collectFromIconify signature to object input"
```

---

### Task 10: PR 作成

**Files:**
- なし (git commands のみ)

**Interfaces:** none

- [ ] **Step 1: 最終ローカル確認**

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm -F @icon-collection/web build
pnpm -F icon-collection build
pnpm -F icon-collection package
```

Expected: 全 green、`.vsix` 生成成功

- [ ] **Step 2: branch push**

```bash
git push -u origin feat/revamp-cleanup
```

- [ ] **Step 3: PR 作成**

```bash
gh pr create --title "chore: cleanup and legacy removal (Plan E)" --body "$(cat <<'EOF'
## Summary
- 旧 `extension/` (v0.1.2) と `parsers/` を削除 (Plan D/B の新実装に置換済み)
- 旧 URL スキーム `/{c}/{n}.svg` → `/icon/{c}/{n}.svg` の 301 redirect を `apps/web/public/_redirects` に追加
- Plan C/D の whole-branch review で挙がった Minor follow-up を一括解消:
  - VSCode extension: Tailwind pipeline 配線 (`@tailwindcss/cli`)、`sanitizeApiBaseUrl` regression テスト (userinfo drop + IPv6)、`.vscodeignore` に CHANGELOG 再包含ペア
  - apps/web: 未使用 `happy-dom` / `ICON_DEFAULT_WIDTH` 除去、`cloudflare:test` → `cloudflare:workers` 移行 (可能なら)、`/api/search` の empty query 応答 cache を `no-store` に
  - tools/ingest: `fakeBatchAtomic` helper を `_helpers.ts` に集約、`collectFromIconify` overload を object signature 単一に統合
- root README.md を新構成に合わせて刷新
- DNS 切替手順を `docs/dns-cutover.md` に切り出し

## Test plan
- [ ] `pnpm install --frozen-lockfile` 成功
- [ ] `pnpm lint / typecheck` clean
- [ ] `pnpm test` 全 workspace green
- [ ] `pnpm -F @icon-collection/web build` 成功、`apps/web/dist/_redirects` が copy される
- [ ] `pnpm -F icon-collection package` 成功、`.vsix` の main.css が Tailwind 展開済み
- [ ] Cloudflare Pages preview で `/mdi/home.svg` が 301 → `/icon/mdi/home.svg` を返す

## Follow-up (後日)
- React Icons 独立コレクション対応
- Semantic search 精度向上
- Windows Electron GPU flakiness 対策 (xvfb-run in CI when integration test に載せる時)
EOF
)"
```

- [ ] **Step 4: PR URL を返す**

---

## Post-Plan Follow-ups (Plan F 以降)

- React Icons 独立コレクション対応
- Semantic search (embedding based)
- integration test を CI へ (xvfb-run 等)
